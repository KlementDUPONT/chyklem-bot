<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Dashboard - Mes Serveurs</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <style>
        body { background-color: #23272a; color: white; font-family: 'Segoe UI', sans-serif; }
        .navbar { background-color: #2c2f33; border-bottom: 1px solid #202225; }
        
        .guild-card {
            background-color: #2c2f33;
            border: 1px solid #202225;
            border-radius: 8px;
            text-align: center;
            padding: 20px;
            transition: transform 0.2s;
            height: 100%;
        }
        .guild-card:hover {
            transform: translateY(-5px);
            background-color: #35393e;
            border-color: #5865F2;
        }
        .guild-icon {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 15px;
            border: 2px solid #23272a;
            background-color: #7289da;
        }
        .btn-config {
            background-color: #5865F2;
            color: white;
            width: 100%;
            border-radius: 5px;
            font-weight: bold;
        }
        .btn-config:hover { background-color: #4752c4; color: white; }
        .btn-invite { background-color: #43b581; color: white; width: 100%; }
        .btn-invite:hover { background-color: #3ca374; color: white; }
        
        .user-info img { width: 35px; height: 35px; border-radius: 50%; margin-right: 10px; }
    </style>
</head>
<body>

    <nav class="navbar navbar-dark py-3">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/"><i class="fas fa-robot"></i> <%= bot.username %></a>
            <div class="d-flex align-items-center user-info">
                <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="Avatar">
                <span class="fw-bold me-3"><%= user.username %></span>
                <a href="/logout" class="btn btn-outline-danger btn-sm"><i class="fas fa-sign-out-alt"></i></a>
            </div>
        </div>
    </nav>

    <div class="container mt-5">
        <h2 class="mb-4 fw-bold">Sélectionne un serveur</h2>
        
        <div class="row g-4">
            <% guilds.forEach(guild => { %>
                <div class="col-12 col-md-6 col-lg-3">
                    <div class="guild-card">
                        <% if (guild.icon) { %>
                            <img class="guild-icon" src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png" alt="<%= guild.name %>">
                        <% } else { %>
                            <div class="guild-icon d-flex align-items-center justify-content-center mx-auto fs-3 text-white">
                                <%= guild.name.charAt(0) %>
                            </div>
                        <% } %>
                        
                        <h5 class="text-truncate mb-3" title="<%= guild.name %>"><%= guild.name %></h5>
                        
                        <% if (guild.botInGuild) { %>
                            <a href="/settings/<%= guild.id %>" class="btn btn-config">Configurer</a>
                        <% } else { %>
                            <a href="https://discord.com/api/oauth2/authorize?client_id=<%= bot.id %>&permissions=8&scope=bot%20applications.commands&guild_id=<%= guild.id %>" target="_blank" class="btn btn-invite">Inviter</a>
                        <% } %>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

</body>
</html>