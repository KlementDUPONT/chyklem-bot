<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= bot.username %> - Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <style>
        body {
            background-color: #23272a;
            color: white;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            text-align: center;
        }
        .hero {
            padding: 100px 20px;
            background: linear-gradient(180deg, #2c2f33 0%, #23272a 100%);
        }
        .btn-login {
            background-color: #5865F2;
            color: white;
            padding: 15px 40px;
            font-size: 1.5rem;
            border-radius: 50px;
            text-decoration: none;
            transition: transform 0.2s;
            font-weight: bold;
        }
        .btn-login:hover {
            transform: scale(1.05);
            background-color: #4752c4;
            color: white;
        }
        .avatar {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            border: 5px solid #FFB6C1;
            margin-bottom: 30px;
        }
    </style>
</head>
<body>

    <div class="hero">
        <img src="<%= bot.displayAvatarURL() %>" class="avatar" alt="Bot Avatar">
        <h1 class="display-3 fw-bold mb-3"><%= bot.username %></h1>
        <p class="lead mb-5 text-muted">Le bot Kawaii ultime pour ton serveur Discord.</p>
        
        <% if (user) { %>
            <a href="/dashboard" class="btn-login">
                <i class="fas fa-tachometer-alt me-2"></i> Accéder au Dashboard
            </a>
            <br><br>
            <a href="/logout" class="text-muted text-decoration-none">Se déconnecter</a>
        <% } else { %>
            <a href="/login" class="btn-login">
                <i class="fab fa-discord me-2"></i> Se connecter avec Discord
            </a>
        <% } %>
    </div>

</body>
</html>