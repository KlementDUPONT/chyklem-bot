<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Configuration - <%= guild.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { background-color: #23272a; color: white; font-family: 'Segoe UI', sans-serif; }
        .navbar { background-color: #2c2f33; border-bottom: 1px solid #202225; }
        .card { background-color: #2c2f33; border: 1px solid #202225; margin-bottom: 20px; }
        .form-label { font-weight: bold; color: #b9bbbe; }
        .btn-save { background-color: #43b581; color: white; border: none; padding: 10px 30px; font-size: 1.1rem; }
        .btn-save:hover { background-color: #3ca374; color: white; }
        .form-select, .form-control { background-color: #23272a; border: 1px solid #202225; color: white; }
        .form-select:focus, .form-control:focus { background-color: #23272a; color: white; border-color: #5865F2; box-shadow: none; }
    </style>
</head>
<body>

    <nav class="navbar navbar-dark py-3 mb-4">
        <div class="container">
            <a class="navbar-brand" href="/dashboard"><i class="fas fa-arrow-left"></i> Retour au Dashboard</a>
            <span class="navbar-text fw-bold text-white"><%= guild.name %></span>
        </div>
    </nav>

    <div class="container">
        <% if (success) { %>
            <div class="alert alert-success"><i class="fas fa-check-circle"></i> Modifications sauvegardées !</div>
        <% } %>

        <form action="/settings/<%= guild.id %>" method="POST">
            
            <div class="row">
                <div class="col-md-6">
                    <div class="card p-4">
                        <h4 class="mb-4 text-primary"><i class="fas fa-door-open"></i> Bienvenue & Rôles</h4>
                        
                        <div class="mb-3">
                            <label class="form-label">Salon de Bienvenue (Image)</label>
                            <select name="welcome_channel_id" class="form-select">
                                <option value="">-- Aucun --</option>
                                <% channels.forEach(channel => { %>
                                    <% if (channel.type === 0) { %>
                                        <option value="<%= channel.id %>" <%= settings.welcome_channel_id === channel.id ? 'selected' : '' %>>
                                            # <%= channel.name %>
                                        </option>
                                    <% } %>
                                <% }) %>
                            </select>
                            <div class="form-text text-muted">Le bot enverra l'image Kawaii ici.</div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Rôle Automatique (Auto-Role)</label>
                            <select name="autorole_id" class="form-select">
                                <option value="">-- Aucun --</option>
                                <% roles.forEach(role => { %>
                                    <% if (role.name !== '@everyone' && !role.managed) { %>
                                        <option value="<%= role.id %>" <%= settings.autorole_id === role.id ? 'selected' : '' %>>
                                            @<%= role.name %>
                                        </option>
                                    <% } %>
                                <% }) %>
                            </select>
                            <div class="form-text text-muted">Donné automatiquement aux nouveaux arrivants.</div>
                        </div>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="card p-4">
                        <h4 class="mb-4 text-danger"><i class="fas fa-shield-alt"></i> Sécurité Anti-Raid</h4>
                        
                        <div class="form-check form-switch mb-3">
                            <input class="form-check-input" type="checkbox" id="antiraid" name="antiraid_enabled" <%= settings.antiraid_enabled ? 'checked' : '' %>>
                            <label class="form-check-label form-label" for="antiraid">Activer l'Anti-Raid</label>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Âge minimum du compte (Jours)</label>
                            <input type="number" name="antiraid_days" class="form-control" value="<%= settings.antiraid_account_age_days || 7 %>" min="1" max="30">
                            <div class="form-text text-muted">Les comptes créés il y a moins de X jours seront expulsés.</div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="text-center mt-3 mb-5">
                <button type="submit" class="btn btn-save rounded-pill shadow">
                    <i class="fas fa-save me-2"></i> Sauvegarder les changements
                </button>
            </div>

        </form>
    </div>

</body>
</html>